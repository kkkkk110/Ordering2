import Vue from 'vue';
import Axios from 'axios';

const state = {

}

const mutations = {
$(document).click(function(e) { // 在页面任意位置点击而触发此事件
  $(e.target).attr("id");       // e.target表示被点击的目标
})

	
}


export default {
	state,

	mutations
}